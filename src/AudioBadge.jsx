export default function AudioBadge({ children }) {
  return (
    <div className="rounded-full bg-origen-mostaza/20 px-4 py-2 text-xs font-black uppercase tracking-[0.2em] text-origen-granate shadow-sm sm:px-5 sm:py-3">
      {children}
    </div>
  )
}
